_Token Passing_ is a variant of a [[Controlled-Access Protocol|controlled-access protocol]] where a **control token** is passed around the network.

The machine with that **control token** is then the only one that is allowed to transmit data at any given moment.

![[Token Passing.png]]