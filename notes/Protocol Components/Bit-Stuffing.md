_Bit-Stuffing_ is a general term describing the insertion of non-informational bits into a packet.

> [!note]
> It is typically used to mark the beginning and/or end of a frame when techniques like variable-length framing are used

> [!example]
> The presence of a null terminating byte ('\0') in C strings is an example of _bit stuffing_